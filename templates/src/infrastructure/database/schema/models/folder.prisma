/// Папка для организации наборов
/// 
/// Модель для создания папок пользователями для организации своих наборов.
/// Позволяет группировать наборы по темам, уровням или другим критериям.
/// Поддерживает иерархическую организацию контента.
model Folder {
  /// Уникальный идентификатор папки
  id          String @id @default(cuid())
  
  /// Дата и время создания папки
  createdAt   DateTime @default(now())
  
  /// Дата и время последнего обновления папки
  updatedAt   DateTime @updatedAt
  
  /// Название папки
  name        String
  
  /// Описание папки
  description String?
  
  /// ID пользователя, создавшего папку
  userId      String
  
  /// Связь с пользователем
  user        User @relation(fields: [userId], references: [id], onDelete: Cascade)

  /// Наборы в этой папке
  sets        Set[] @relation("sets_folders")

  @@map("folders")
  @@index([userId])
  @@index([name])
}
