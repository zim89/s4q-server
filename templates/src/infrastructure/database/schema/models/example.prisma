/// Пример использования слова или фразы
/// 
/// Модель для хранения примеров использования карточек.
/// Поддерживает систему голосования для определения качества примеров.
/// Позволяет иметь несколько примеров для одной карточки.
model Example {
  /// Уникальный идентификатор примера
  id         String   @id @default(cuid())
  
  /// Дата и время создания примера
  createdAt  DateTime @default(now())
  
  /// Дата и время последнего обновления примера
  updatedAt  DateTime @updatedAt

  /// Текст примера использования
  content    String
  
  /// Количество положительных голосов
  upVotes    Int @default(0)
  
  /// Количество отрицательных голосов
  downVotes  Int @default(0)
  
  /// Карточки, к которым относится этот пример
  cards      Card[] @relation("cards_examples")

  @@map("examples")
  @@index([content])
  @@index([upVotes, downVotes])
}
