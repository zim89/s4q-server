/// Тег для категоризации
/// 
/// Модель для создания тегов для категоризации наборов и карточек.
/// Позволяет группировать контент по темам, уровням сложности, жанрам.
/// Упрощает поиск и фильтрацию контента.
model Tag {
  /// Уникальный идентификатор тега
  id         String @id @default(cuid())
  
  /// Дата и время создания тега
  createdAt  DateTime @default(now())
  
  /// Дата и время последнего обновления тега
  updatedAt  DateTime @updatedAt

  /// Название тега (например, "Beginner", "Business", "Travel")
  name       String
  
  /// URL-дружественный идентификатор тега
  slug       String @unique

  /// Наборы с этим тегом
  sets       Set[]

  @@map("tags")
  @@index([name])
  @@index([slug])
}
