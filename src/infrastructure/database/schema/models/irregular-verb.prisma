/// Неправильный глагол
/// 
/// Модель для хранения неправильных глаголов с их формами.
/// Содержит только глаголы, которые не следуют стандартным правилам спряжения.
/// Связывает карточки с соответствующими формами неправильных глаголов.
model IrregularVerb {
  /// Уникальный идентификатор неправильного глагола
  id              String @id @default(cuid())
  
  /// Дата и время создания записи
  createdAt       DateTime @default(now())
  
  /// Дата и время последнего обновления записи
  updatedAt       DateTime @updatedAt

  /// Инфинитив глагола (базовая форма)
  infinitive      String
  
  /// Простое прошедшее время (Past Simple)
  pastSimple      String
  
  /// Причастие прошедшего времени (Past Participle)
  pastParticiple  String
  
  /// Перевод на русский язык
  translation     String
  
  /// Уровень сложности (A1, A2, B1, B2, C1, C2)
  level           LanguageLevel
  
  /// Тип глагола
  verbType        VerbType @default(IRREGULAR)
  
  /// Фонетическая транскрипция инфинитива
  transcription   String?
  
  /// Частота использования (1-10, где 10 - самая частая)
  frequency       Int? @default(5)
  
  /// Сложность запоминания (1-10, где 10 - самая сложная)
  difficulty      Int? @default(5)
  
  /// Происхождение глагола (этимология)
  etymology       String?
  
  /// Дополнительные заметки
  notes           String?

  // Связи с другими моделями
  /// Карточки, связанные с этим неправильным глаголом
  cards           Card[]

  @@map("irregular_verbs")
  @@unique([infinitive])
  @@index([level])
  @@index([verbType])
  @@index([frequency])
  @@index([difficulty])
  @@index([translation])
} 
