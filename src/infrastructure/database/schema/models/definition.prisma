/// Определение слова или фразы
/// 
/// Модель для хранения определений карточек.
/// Поддерживает систему голосования для определения качества определений.
/// Позволяет иметь несколько определений для одной карточки.
model Definition {
  /// Уникальный идентификатор определения
  id         String   @id @default(cuid())
  
  /// Дата и время создания определения
  createdAt  DateTime @default(now())
  
  /// Дата и время последнего обновления определения
  updatedAt  DateTime @updatedAt

  /// Текст определения
  content    String
  
  /// Количество положительных голосов
  upVotes    Int @default(0)
  
  /// Количество отрицательных голосов
  downVotes  Int @default(0)
  
  /// Карточки, к которым относится это определение
  cards      Card[] @relation("cards_definitions")

  @@map("definitions")
  @@index([content])
  @@index([upVotes, downVotes])
}
