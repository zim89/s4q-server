/// Достижение пользователя
/// 
/// Модель для системы достижений пользователей.
/// Отслеживает достижения пользователей за различные действия:
/// изучение карточек, завершение наборов, серии дней изучения.
/// Мотивирует пользователей к регулярному изучению.
model Achievement {
  /// Уникальный идентификатор достижения
  id          String   @id @default(cuid())
  
  /// Дата и время создания записи
  createdAt   DateTime @default(now())
  
  /// Тип достижения
  type        AchievementType
  
  /// Название достижения (например, "Completed 100 Cards")
  name        String
  
  /// Описание достижения
  description String?
  
  /// Дополнительные данные достижения в формате JSON
  data        Json?
  
  /// Дата и время получения достижения
  earnedAt    DateTime?
  
  /// ID пользователя
  userId      String
  
  /// Связь с пользователем
  user        User     @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@map("achievements")
  @@index([userId])
  @@index([earnedAt])
  @@index([name])
}
