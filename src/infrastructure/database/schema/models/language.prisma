/// Язык
/// 
/// Модель для хранения поддерживаемых языков в системе.
/// Определяет языки, для которых доступны карточки и наборы.
/// Используется для фильтрации и организации контента по языкам.
model Language {
  /// Уникальный идентификатор языка
  id         String @id @default(cuid())
  
  /// Дата и время создания записи
  createdAt  DateTime @default(now())
  
  /// Название языка (например, "English", "Deutsch")
  name       String
  
  /// Код языка по ISO 639-1 (например, "en", "de")
  code       String @unique
  
  /// Активен ли язык в системе
  isEnabled  Boolean @default(true)

  // Связи с другими моделями
  /// Карточки на этом языке
  cards      Card[]
  
  /// Наборы на этом языке
  sets       Set[]

  @@map("languages")
  @@index([isEnabled])
  @@index([code])
}
