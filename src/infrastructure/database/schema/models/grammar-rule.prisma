/// Грамматическое правило
/// 
/// Модель для хранения грамматических правил языка.
/// Связывает карточки с соответствующими грамматическими правилами.
/// Помогает в изучении грамматики через контекст карточек.
model GrammarRule {
  /// Уникальный идентификатор правила
  id          String @id @default(cuid())
  
  /// Дата и время создания правила
  createdAt   DateTime @default(now())
  
  /// Дата и время последнего обновления правила
  updatedAt   DateTime @updatedAt

  /// Название грамматического правила
  name        String
  
  /// URL-дружественный идентификатор правила
  slug        String @unique
  
  /// Описание грамматического правила
  description String?

  /// Карточки, связанные с этим правилом
  cards       Card[]

  @@map("grammar_rules")
  @@index([slug])
  @@index([name])
}
